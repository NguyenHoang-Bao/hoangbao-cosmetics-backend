spring:
  application:
    name: hoangbaocosmetics_backend
  datasource:
    url: jdbc:mysql://localhost:3306/hoangbao_cosmetics_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: "NhB0359490221*"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# Cấu hình JWT cho ứng dụng
jwt:
  # Secret key tối thiểu 256-bit (chuỗi Base64)
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  # Thời hạn token: 86400000 ms = 24 giờ = 1 ngày
  expiration-ms: 86400000

# Cấu hình thanh toán VietQR & SePay Webhook
payment:
  vietqr:
    bank-id: "MBBank" # Mã ngân hàng (MBBank, VCB, ACB, TPBank, TCB, VPB,...)
    account-number: "14580886868" # Số tài khoản ngân hàng nhận tiền
    account-name: "NGUYEN HOANG BAO" # Tên chủ tài khoản
    template: "compact2" # Template hiển thị mã QR: compact, compact2, qr_only
  sepay:
    api-key: "SEPAY_API_KEY_SECRET" # API Key do SePay cấp để xác thực Webhook